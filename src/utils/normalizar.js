// TODO Parte A: construir el arreglo normalizado segun el formato de documento.md
// (id, nombre, estado, especie, tipo, genero, origen, ubicacionActual, cantidadEpisodios, imagen)
export function normalizarPersonajes(personajes) {
  throw new Error('normalizarPersonajes no implementado')
}
