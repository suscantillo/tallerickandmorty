// recibe siempre el arreglo ya normalizado

// TODO Parte B.6 (reduce): agrupar por especie con cantidad, promedio de episodios y vivos
export function agruparPorEspecie(personajes) {
  throw new Error('agruparPorEspecie no implementado')
}

// TODO Parte B.7 (reduce): clasificar por rango de episodios (1-5, 6-15, 16-30, 30+)
export function clasificarPorEpisodios(personajes) {
  throw new Error('clasificarPorEpisodios no implementado')
}
